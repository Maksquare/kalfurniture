import ContactContent from "./ContactContent";

export const metadata = {
  title: "Contact Us | Phoenix Furniture",
  description: "Get in touch with Phoenix Furniture for design consultations and showroom visits.",
};

const ContactPage = () => {
  return <ContactContent />;
};

export default ContactPage;
